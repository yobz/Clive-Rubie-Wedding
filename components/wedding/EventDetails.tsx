import { Church,Wine,MapPin,ArrowUpRight,CalendarPlus } from 'lucide-react';
import { weddingData,type WeddingEvent } from '@/lib/wedding-data';
function calendar(e:WeddingEvent){const text=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//Isabel and Mateo//Wedding//EN','BEGIN:VEVENT',`UID:${e.start}@isabel-mateo-wedding`,'DTSTAMP:20260909T000000Z',`DTSTART:${e.start}`,`DTEND:${e.end}`,`SUMMARY:Isabel & Mateo - ${e.title}`,`LOCATION:${e.venue}\\, Intramuros Manila`,'END:VEVENT','END:VCALENDAR',''].join('\r\n');const url=URL.createObjectURL(new Blob([text],{type:'text/calendar;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=`wedding-${e.start}.ics`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
export function EventDetails(){return <section id="details" className="section events"><div className="section-heading"><span className="eyebrow">02 / KAGANAPAN</span><h2>A day to remember</h2><p>From “I do” to a dance under the stars.</p></div><div className="event-grid">{weddingData.events.map((e,i)=><article className="event-card" key={e.title}><div className="event-top">{i===0?<Church size={32} strokeWidth={1}/>:<Wine size={32} strokeWidth={1}/>}<span className="eyebrow">{e.time}</span></div><p className="event-label">{e.title}</p><h3>{e.venue}</h3><p className="address"><MapPin size={15}/>{e.address}</p><div className="card-actions"><a href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(e.query)}`} target="_blank" rel="noreferrer">View location <ArrowUpRight size={15}/></a><button onClick={()=>calendar(e)}><CalendarPlus size={15}/> Add to calendar</button></div></article>)}</div><p className="small-note">Please arrive 30 minutes before the ceremony. We can’t wait to see you.</p></section>}
